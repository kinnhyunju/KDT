## str 데이터 타입의 특징 -------------------------------------------------------
1. 인덱싱 & 슬라이싱으로 원소/요소 접근
2. 원소/요소 단위 변경 즉, 수정과 삭제 불가
3. 산술 연산자 중에 덧셈(+) => str + str => 문자열 하나로 연결
                   곱셈(*) => str + int => 정수만큼 반복해서 문자열 연결
4. 멤버 연산자 in, not in 사용
5. 내장함수 len(), ord(문자1개), chr(코드1개)